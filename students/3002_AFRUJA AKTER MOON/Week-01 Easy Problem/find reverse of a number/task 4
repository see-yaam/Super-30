#include <stdio.h>

int main()
{
    int n, reverse = 0, remove;

    printf("Enter a number: ");
    scanf("%d", &n);

    for( ; n > 0; n = n / 10)
    {
        remove = n % 10;
        reverse= reverse*10 + remove;
    }

    printf("Reverse number = %d", reverse);

    return 0;
}
