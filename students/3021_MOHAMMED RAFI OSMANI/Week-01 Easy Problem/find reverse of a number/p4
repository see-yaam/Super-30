#include <stdio.h>

int main(){
    int num;
int reverse = 0;

    printf("Enter any number to find reverse: ");
    scanf("%d", &num);

    for(; num != 0; num /= 10)
    {
        reverse = (reverse * 10) + (num % 10);
    }

    printf("Reverse = %d", reverse);

    return 0;
}