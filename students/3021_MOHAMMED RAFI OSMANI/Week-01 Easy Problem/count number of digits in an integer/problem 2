#include <stdio.h>

int main(){
    int num;
   int count = 0;

  printf("Input num: ");
  scanf("%d", &num);

    while(num != 0){
    
        count++;
        num = num / 10;
    }

    printf("Total digits: %d", count);

    return 0;
}