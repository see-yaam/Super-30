#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, n;
    cin >> x >> n;
    
    int sum = 0;
    for (int i = 1, sign = 1, exp = 1; i <= n; i++, exp += 2, sign = -sign) {
        sum += sign * pow(x, exp);
    }
    
    cout << sum << endl;
    return 0;
}