#include<iostream>
using namespace std;

int main()
{
    int n;
    cout<<"Give a number: ";
    cin>>n;
    int reverse=0;
    while(n!=0){
        int x = n%10;
        reverse = (reverse*10) + x;
        n = n/10;
    }
    cout<<"Reverse number is : "<<reverse;
}