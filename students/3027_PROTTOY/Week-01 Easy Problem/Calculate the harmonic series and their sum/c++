#include<iostream>
using namespace std;

int main()
{
    int n;
    cout<<"Enter number of terms: ";
    cin>>n;
    
    double sum = 0;
    
    for(int i=1;i<=n;i++)
    {
        sum = sum + 1/(double)i;
        
    }
    
    cout<<"Sum of series upto "<< n << "terms: "<<sum;
}